# Redacted Infrastructure Verification Snapshot

**Captured:** 2026-07-31 09:30:55 UTC  
**Purpose:** evidence of the exact state after the initial build and deliberate Coolify pause  
**Secrets:** none included

## Host

```text
Hostname: v2202607389872491325.supersrv.de
Operating system: Ubuntu 24.04.4 LTS
Kernel: 6.8.0-136-generic
Architecture: x86_64
Timezone: UTC
CPU cores: 8
Memory: 15 GiB
Swap: 8.0 GiB
Root filesystem: 503 GiB total, 14 GiB used, 469 GiB available, 3% used
Failed systemd units: 0
```

## Container platform

```text
Docker Engine: 29.7.0
Docker Compose: v5.3.1
Coolify image: docker.io/coollabsio/coolify:4.1.2
Running containers: 0
```

Present stopped containers:

```text
coolify
coolify-db
coolify-proxy
coolify-realtime
coolify-redis
coolify-sentinel
```

The non-zero exit statuses on `coolify` and `coolify-realtime` reflect the deliberate Compose stop signal during the pause. They were healthy before the pause.

## SSH

```text
Port: 22
PermitRootLogin: prohibit-password / key only
Public-key authentication: yes
Password authentication: no
Keyboard-interactive authentication: no
X11 forwarding: no
Agent forwarding: no
TCP forwarding: yes
Maximum authentication attempts: 3
```

The dedicated authorized key was verified after SSH reload. Its fingerprint is:

```text
SHA256:/Yjy3IfjFbrGFhNxqMv6T1xX0hiQ1k+lKbIrb4R2Wrs
```

## Firewall

UFW is active with default-deny inbound and default-allow outbound behavior.

Configured TCP rules for IPv4 and IPv6:

```text
22    rate limited    SSH
80    allow           HTTP
443   allow           HTTPS
8000  allow           temporary Coolify setup
6001  allow           temporary Coolify realtime
6002  allow           temporary Coolify terminal
```

Observed listeners while Coolify was paused:

```text
TCP 22 on IPv4 and IPv6
Local systemd-resolved DNS listeners only
```

No public Coolify listener remained. A direct check to TCP 8000 failed as expected.

## Persistence and maintenance

```text
/swapfile none swap sw 0 0
vm.swappiness = 10
vm.vfs_cache_pressure = 50
Automatic package-list refresh: enabled daily
Unattended security upgrades: enabled daily
fstrim.timer: enabled
```

## Coolify installation evidence

```text
Installer source: https://cdn.coollabs.io/coolify/install.sh
Installer SHA-256: 58132d98fe956d1a16df378fd22250153b6fbc08a84e044d80da3324450a0ca3
Installation log: /data/coolify/source/installation-20260731-092229.log
Upgrade log: /data/coolify/source/upgrade-2026-07-31-09-22-48.log
Wrapper log: /root/coolify-install.log
```

## Deliberately absent

```text
Coolify root administrator: not created
Custom domain and HTTPS: not configured
Provider firewall: not configured
Off-server Coolify backup: not created
Netcup snapshot: not created
OpenViking: not deployed
Hermes: not deployed
Client data and credentials: not migrated
```
